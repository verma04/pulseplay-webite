/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['pulseplaydigital.com' , 'pulseplay-website.s3.amazonaws.com'],
  },
}

module.exports = nextConfig
